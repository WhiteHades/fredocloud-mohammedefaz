{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node --watch src/server.js",
    "build": "rm -rf dist && mkdir -p dist && cp -r src prisma package.json dist/",
    "start": "node src/server.js",
    "lint": "node -e \"process.exit(0)\"",
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "prisma": "^7.8.0",
    "supertest": "^7.2.2"
  },
  "dependencies": {
    "@prisma/client": "^7.8.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  }
}
